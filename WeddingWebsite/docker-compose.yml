services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: weddingwebsite
    environment:
      ASPNETCORE_HTTP_PORTS: "8080"
    ports:
      - "8080:8080"
    restart: unless-stopped
